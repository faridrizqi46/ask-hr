# Plan: Setup AskHR CV Analyzer Locally

## Completed Tasks
- [x] Create `.env` file from `.env.txt`
- [x] Install dependencies
- [x] Push Prisma schema to database
- [x] Start development server
- [x] Fix login redirect to `/dashboard/chat`
- [x] Rename `cv-analyzer-test` folder to `cv-analyzer`
- [x] Fix sidebar route from `cv-analyzer-test` to `cv-analyzer`

## Current Task: Make CV Analyzer Page Cleaner

### Problem
The CV Analyzer page has borders that are too sharp and prominent, making it look unpleasant to view.

### Changes to Make in `src/app/dashboard/cv-analyzer/page.tsx`

1. **Job List Panel (line 208)** - Remove hard border
   - Change `border-r bg-card` to softer styling
   - Add subtle shadow or background differentiation instead

2. **Upload Area (line 253-254)** - Reduce border prominence
   - Change `border-2 border-dashed` to lighter border
   - Use softer colors for the dashed border

3. **Job Card Items (line 220)** - Soften card borders
   - Change `border bg-background` to softer rounded style
   - Consider using subtle shadow or muted background

4. **Overall Layout Dividers (line 253)** - Reduce hard dividers
   - Change `border-r` to softer separation
   - Use background color differences instead

5. **Card Components** - Reduce card borders
   - Use shadow instead of border for cards
   - Soften the overall card appearance

### Design Approach
- Use subtle shadows instead of borders for separation
- Soften colors using `bg-muted/50` or similar
- Reduce border prominence (use `border-border` instead of `border`)
- Add more whitespace and breathing room
- Use rounded corners more consistently
